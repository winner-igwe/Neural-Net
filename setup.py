from setuptools import setup, find_packages

setup(
    name='nnfs',
    version='0.1',
    packages=find_packages(),
    install_requires=['numpy'],
    author='Your Name',
    description='Neural Network from Scratch Library',
)
